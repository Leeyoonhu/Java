<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.ai.mapper.BoardMapper">
	<select id="getList" resultType="org.ai.domain.BoardVO">
		select * from board where boardTitle = #{title}
	</select>
	
	<select id="getNoticeList" resultType="org.ai.domain.BoardVO">
		select * from board
	</select>
	
	<select id="getPopularList" resultType="org.ai.domain.BoardVO">
		select * from board order by recommends desc limit 5
	</select>
	
	<select id="getCurrentList" resultType="org.ai.domain.BoardVO">
		select * from board order by regDate desc limit 5
	</select>
	
	<insert id="write">
		insert into board (title, content, writer, imageFileName, fileName, boardTitle) values
		(#{title}, #{content}, #{writer}, #{imageFileName}, #{fileName}, #{boardTitle})
	</insert>
	
	<select id="getBoardView" resultType="org.ai.domain.BoardVO">
		select * from board where number = #{number}
	</select>
	
	<select id="search" resultType="org.ai.domain.BoardVO">
		select * from board where ${searchTitle} LIKE CONCAT('%',#{content},'%')
	</select>
</mapper>